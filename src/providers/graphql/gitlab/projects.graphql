query UserRepositories($userName: ID!) {
    namespace(fullPath: $userName) {
    projects(includeSubgroups: true) {
      edges {
        node {
          archived,
          path,
          sshUrlToRepo,
          repository {
            rootRef
          }
        }
      }
    }
  }
}

query AllProjectsQuery($group: ID!) {
  group(fullPath: $group) {
    id,
    projects(includeSubgroups: true) {
      edges {
        node {
          id,
          fullPath,
          sshUrlToRepo,
          repository {
            rootRef
          }
        }
      }
    }
  }
}